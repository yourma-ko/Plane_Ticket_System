package entities;
/*public class BusinessClassUser extends User{
    private double additionalBaggage;
    public BusinessClassUser(String name, Integer age, Double balance){
        super(name, age, balance);
        additionalBaggage = 0;
    }
    public void setAdditionalBaggage(double additionalBaggage) {
        this.additionalBaggage = additionalBaggage;
    }
    public double getAdditionalBaggage() {
        return additionalBaggage;
    }
    /*@Override
    public boolean buyTicket(Ticket ticket) {
        double BuisnessPrice = ticket.getEconomyClassPrice() + 10000;
        if (getBalance() >= BuisnessPrice) {
            setBalance(getBalance() - BuisnessPrice);
            getPurchasedTickets().add(ticket);
            ticket.setUserName(getName());
            return true;
        } else {
            System.out.println("Not enough balance to buy the ticket.");
            return false;
        }
    }*/
    /*@Override
    public void cancelTicket(Ticket ticket) {
        super.balance += (ticket.getEconomyClassPrice()+10000);
        super.purchasedTickets.remove(ticket);
    }
}*/

